<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title>{{page_title}}</title>
  </head>
  <body>
    <h1>{{page_title}}</h1>

    {% include 'partial' %}

    {% for item in arrayOfStuff %}
      <p>{{item}}</p>
    {% endfor %}

    {% for item in iterativeWithObjects %}
      <p>{{item.color}}</p>
    {% endfor %}
  </body>
</html>
